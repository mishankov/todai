version: "3"

dotenv: [".env"]

includes:
  backend:
    taskfile: ./backend/Taskfile.yml
    dir: ./backend
  frontend:
    taskfile: ./frontend/Taskfile.yml
    dir: ./frontend
  infrastructure:
    taskfile: ./infrastructure/Taskfile.yml
    dir: ./infrastructure
  pi-runner:
    taskfile: ./pi-runner/Taskfile.yml
    dir: ./pi-runner

tasks:
  setup:
    desc: Install project dependencies
    cmds:
      - task: frontend:install
      - task: pi-runner:install

  setup:test:
    desc: Install project dependencies and test browsers
    cmds:
      - task: frontend:install
      - task: pi-runner:install
      - task: frontend:browsers

  dev:
    desc: Start local infrastructure and run development servers
    cmds:
      - task: infrastructure:up
      - task: backend:migrate
      - task: pi-runner:build
      - task: dev:servers

  dev:servers:
    internal: true
    deps:
      - backend:dev
      - frontend:dev

  check:
    desc: Run all checks that do not require external services
    cmds:
      - task: pi-runner:check
      - task: backend:check
      - task: frontend:check
